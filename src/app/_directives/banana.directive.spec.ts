import { BananaDirective } from './banana.directive';

describe('BananaDirective', () => {
  it('should create an instance', () => {
    const directive = new BananaDirective();
    expect(directive).toBeTruthy();
  });
});
